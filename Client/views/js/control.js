'use strict';

let directory = $('#directory').text();
//const port = require(directory + '/port.js');

$(document).ready(() => {
  $('#expresso_i').click(() => {
    console.log('Test');
  });

  $('#coffee_i').click(() => {

  });

  $('#backe_i').click(() => {

  });

  $('#backc_i').click(() => {

  });
});
